query GetCourseBySlug($slug: String) {
  Courses(where: { slug: { equals: $slug } }) {
    docs {
      kinescopeVideos

      ...CourseFragment
    }
  }
}

query GetCourseSlugById($id: Int!) {
  Course(id: $id) {
    slug
  }
}
